<%= simple_form_for [:backend, @speaker] do |f| %>
  <%= f.input :name %>
  <%= f.input :avatar, as: :file%>
  <%= f.input :biography %>
  <%= f.input :subject %>
  <%= f.input :abstract %>
    
  <%= f.submit class: "btn btn-primary"%>
<% end %>